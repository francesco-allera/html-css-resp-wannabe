# EXERCISE 10 - BOOLEAN CAREERS TRAINEE:


### Languages used:

- HTML5
- CSS3


### Purpose of the exercise:

First approach of the concept of responsive web design.

The HTML and CSS files was given to get modify only on responsiveness.

The only part enabled for modifications were the @media query.

The exercise is desktop first oriented, so the CSS rules go to LG -> MD -> SM -> XS.
